# iris-ml-project
This project uses supervised machine learning in R to classify Iris flower species using the Iris dataset. It compares Multinomial Logistic Regression and Random Forest models, evaluates them with a train/test split and confusion matrix, and includes a reproducible R script, R Markdown report, and PDF output.
